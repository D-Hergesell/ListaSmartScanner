<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!--
        Para testar contra um backend LOCAL via http:// (ex.: API_BASE_URL=http://10.0.2.2:8080/)
        é preciso permitir tráfego em texto puro. NÃO deixe isto ligado em produção (use só https).
        Para habilitar apenas em debug, crie app/src/debug/AndroidManifest.xml com:
            android:usesCleartextTraffic="true"
        Em produção (Render) a URL é https, então nada precisa ser alterado aqui.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListaSmart">

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.ScannerActivity"
            android:exported="false" />

    </application>

</manifest>
